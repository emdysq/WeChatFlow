services:
  wechatflow:
    build: .
    ports:
      - "8000:8000"
    env_file:
      - .env
    environment:
      DATABASE_URL: sqlite:///./data/wechatflow.db
      UPLOAD_DIR: uploads
    volumes:
      - ./uploads:/app/uploads
      - ./data:/app/data
